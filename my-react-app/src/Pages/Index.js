function Index({name, age, children}) {
    return (
        <div>
            <h1>{name}</h1>
            <h2>{age}</h2>
            <h3>{children}</h3>
        </div>
    );
}

export default Index;